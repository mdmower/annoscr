build/
node_modules/
*.tsbuildinfo

# Release artifacts collected by `npm run dist`
dist/

# Claude Code context
CLAUDE.md
CLAUDE.local.md
.claude/

# Debian packaging artifacts
debian/.debhelper/
debian/files
debian/*.substvars
debian/*.log
debian/annoscr/
debian/debhelper-build-stamp

# Arch makepkg build artifacts (makepkg runs in packaging/arch/)
packaging/arch/pkg/
packaging/arch/src/
packaging/arch/*.pkg.tar.*
packaging/arch/*.tar.gz

# Editor scratch
.vscode/
.idea/
*.swp

# Local meson native file pointing at a developer's gettext ≥0.25 build
# (absolute $HOME path); the committed .example documents the format.
build-aux/xgettext.ini
